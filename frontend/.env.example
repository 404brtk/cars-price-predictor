# ---------------------------------
# Frontend (Next.js) Configuration
# ---------------------------------
# This variable is exposed to the browser and tells your frontend
# where the backend API is located.
NEXT_PUBLIC_API_URL=http://localhost:8000/api

NODE_ENV=production